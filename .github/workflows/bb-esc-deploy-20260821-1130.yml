name: bb esc deploy 20260821-1130

on:
  workflow_dispatch:
    inputs:
      environment:
        description: target environment
        required: true
        type: string

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: ${{ github.event.inputs.environment }}
    steps:
      - name: Marker
        run: echo "bb-esc-deployed ${{ github.event.inputs.environment }}"
