name: grok esc run other 20260821

on:
  push:
    paths:
      - ".github/workflows/grok-esc-run-other-20260821.yml"
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: grok-esc-other-20260821
    steps:
      - name: Marker
        run: echo other-reviewer-marker
