name: grok esc run wait 20260821

on:
  push:
    paths:
      - ".github/workflows/grok-esc-run-wait-20260821.yml"
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: grok-esc-wait-20260821
    steps:
      - name: Marker
        run: echo wait-timer-marker
