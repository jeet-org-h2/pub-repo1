name: BB Pending Deployment 20260725
on:
  workflow_dispatch:
jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: bb-pending-deploy-20260725
    steps:
      - run: echo disposable deployment authorization fixture
