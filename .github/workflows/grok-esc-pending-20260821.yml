name: grok esc pending 20260821

on:
  workflow_dispatch:
    inputs:
      environment:
        description: target environment
        required: true
        type: string

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: ${{ github.event.inputs.environment }}
    steps:
      - name: Marker
        run: echo "grok-esc-approved ${{ github.event.inputs.environment }}"
