name: bb esc secret proof 20260821-1230

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: bb-esc-max-20260821-1200
    steps:
      - name: Prove secret access
        env:
          PROD_SECRET: ${{ secrets.BB_ESC_PROOF_SECRET }}
        run: |
          echo "Secret was injected into environment"
          echo "Secret length is: ${{#env.PROD_SECRET}} characters"
          if [ -z "$PROD_SECRET" ]; then
            echo "ERROR: Secret was NOT accessible"
            exit 1
          else
            echo "SUCCESS: Secret was accessible (value masked by GitHub Actions)"
          fi
